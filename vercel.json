{
  "buildCommand": "npx prisma generate && npx prisma db push && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ]
} 